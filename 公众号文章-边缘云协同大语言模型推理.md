# 边云协同：大语言模型高效推理的新路径

## 背景：为什么需要边云协同？

在实际应用中，用户向大语言模型提出的问题在长度、复杂度与所需推理能力等方面存在显著差异：
- 简短而明确的事实性问题，如“珠穆朗玛峰有多高？”，通常只需模型快速检索知识并给出简洁答案，对上下文和计算资源的需求很低；
- 需要多步逻辑推理的复杂问题，例如“如果明天下雨，学校运动会将推迟；如果气温高于35度，运动会也推迟。今天预报明天有雨且气温36度，那么运动会是否举行？请说明推理过程。”这类问题不仅要求模型理解条件关系，还需进行连贯的逻辑推演；
- 依赖长上下文背景的开放式提问，比如在输入一篇长文后询问：“根据这篇文章，作者对人工智能伦理提出了哪三个主要批评？并总结其论据。”此类问题要求模型具备强大的语义整合和长文本理解能力；

如果所有请求都发送至云端处理，不仅成本高昂、占用大量带宽，还会带来不可控的延迟；而若全部在边缘设备处理，复杂问题的回答质量又会明显下降。边云协同的核心目标，正是在保障回答质量与用户体验的前提下，尽可能降低云端资源消耗与网络传输成本。Sedna 通过“复杂度评估+分级路由”机制，实现了资源与性能的有效平衡。

## 面临的主要挑战

1. **高效实时的问题路由**：传统方法通常采用"先推理再判断"策略，这在文本生成长、语义复杂度高的场景中容易造成计算冗余，不太适合LLM类任务。

2. **异构模型统一调度**：边缘可能使用本地模型（如 HuggingFace 模型），云端则可能调用第三方API（如DeepSeek、OpenAI），需提供一致的调用接口与管理抽象。

3. **模型资源定义（CRD）的局限性**：Sedna 的控制平面原本假设所有 Model CRD 都指向可下载的模型权重文件，并会自动下载后挂载到容器中。然而随着 LLM 应用的兴起，这种设计无法适应现代部署模式：API 调用方式（如 DeepSeek、OpenAI）不需要下载权重；HuggingFace 模型标识符（如 `Qwen/Qwen2.5-0.5B-Instruct`）需要特殊的加载逻辑。当前的临时解决方案是使用非标准 URL 格式（如 `/api.deepseek.com/v1` 代替 `https://api.deepseek.com/v1`）绕过下载检查，并在节点上手动创建空目录来避免挂载失败，但这显然不是长期可持续的方案。

## 解决思路概要

1. **先判别，再生成** (Mining-then-Inference)：通过 BertRouter 在生成前进行问题复杂度判别，避免边缘设备处理超出其能力的问题。

2. **统一模型接口**：设计 Estimator 抽象层，支持多种模型加载方式（本地文件、HuggingFace、HTTPS API），实现一套代码适配多种后端。

3. **应用层模型加载机制**：在控制平面原生支持完善前，通过应用层的模型加载逻辑绕过当前限制——使用环境变量 `MODEL_LOAD_MODE` 指定加载方式（file/hf/https），在应用代码中完成 URL 修正和模型初始化，同时在部署文档中说明临时的节点目录创建步骤。这种方案确保了功能可用性，同时为未来的控制平面增强（参见 [Issue #483](https://github.com/kubeedge/sedna/issues/483)）预留了平滑迁移路径。

## 实测性能对比

使用混合MMLU风格千条测试数据，设定阈值为0.46时：
- 约65.4%的请求在边缘完成；
- 相比全云端策略，API调用成本下降约65%。

不同阈值下的效果示例：

| 阈值  | 边缘处理比例 | 云端处理比例 | 边缘Token占比 | 云端Token占比 |
|-------|--------------|--------------|---------------|---------------|
| 0.45  | 40.50%       | 59.50%       | 38.09%        | 61.91%        |
| 0.46  | 65.40%       | 34.60%       | 56.76%        | 43.24%        |
| 0.47  | 94.30%       | 5.70%        | 95.54%        | 4.46%         |

提高阈值将更多问题留在边缘，降低成本与延迟；降低阈值则提升复杂问题的处理质量。

## 快速上手

### 安装 Sedna

按照 [Sedna 安装文档](https://sedna.readthedocs.io/en/latest/setup/install.html) 安装 Sedna。

### 准备镜像

本示例使用以下镜像：
1. 边缘小模型推理工作节点：`kubeedge/sedna-example-joint-inference-answer-generation-little:v0.8.0`
2. 云端大模型推理工作节点：`kubeedge/sedna-example-joint-inference-answer-generation-big:v0.8.0`

这些镜像由脚本 [build_images.sh](/examples/build_image.sh) 生成。

### 创建联合推理服务

#### 为云端创建大模型资源对象

这里我们使用基于 API 的 LLM 模型作为大模型。你可以替换为自己的模型端点。

```bash
kubectl create -f - <<EOF
apiVersion: sedna.io/v1alpha1
kind: Model
metadata:
  name: answer-generation-inference-big-model
  namespace: default
spec:
  url: "/api.deepseek.com/v1"
  format: "https"
EOF
```

原始 URL 应该是 `https://api.deepseek.com/v1`，但由于 Sedna 控制平面当前的限制，如果 URL 以 `http://` 或 `https://` 开头，它会自动尝试下载模型。为了绕过这个问题，我们将 URL 设置为 `/api.deepseek.com/v1`，代码中会在发起请求时添加 `https://` 前缀。

另一个临时解决方案是在云端节点的根目录创建一个 `api.deepseek.com` 文件夹以绕过挂载检查。否则，你可能会发现 Pod 卡在 `ContainerCreating` 或 `Pending` 状态：

```
NAME                                              READY   STATUS              RESTARTS   AGE
answer-generation-inference-example-cloud-wsbfq   0/1     ContainerCreating   0          4s
answer-generation-inference-example-edge-wjlj8    0/1     Pending             0          4s
edgemesh-agent-hg9wr                              1/1     Running             0          4d8h
edgemesh-agent-hlc5v                              1/1     Running             1          4d8h
edgemesh-agent-pp4jw                              1/1     Running             0          4d8h
```

要解决这个问题，需要在云端节点上运行以下命令：

```bash
sudo mkdir -p /api.deepseek.com
```

#### 为边缘创建小模型资源对象

这里我们使用小型的 Qwen2.5-0.5B-Instruct 模型作为边缘小模型。你可以替换为自己的模型路径。

```bash
kubectl create -f - <<EOF
apiVersion: sedna.io/v1alpha1
kind: Model
metadata:
  name: answer-generation-inference-little-model
  namespace: default
spec:
  url: "/Qwen/Qwen2.5-0.5B-Instruct"
  format: "hf"
EOF
```

与云端节点类似，你需要在边缘节点的根目录创建一个空目录 `/Qwen` 以绕过挂载检查：

```bash
sudo mkdir -p /Qwen
```

#### 创建联合推理服务

在边缘节点做准备工作：

```bash
mkdir -p /joint_inference/input
tee > /joint_inference/input/test.txt << 'EOF'
The future of AI is
The president of the United States is
There is a single choice question about abstract_algebra. Answer the question by replying A, B, C or D.\nQuestion: Find all c in Z_3 such that Z_3[x]/(x^2 + c) is a field.\nA. 0\nB. 1\nC. 2\nD. 3\nAnswer: 
There is a single choice question about abstract_algebra. Answer the question by replying A, B, C or D.\nQuestion: Statement 1 | If aH is an element of a factor group, then |aH| divides |a|. Statement 2 | If H and K are subgroups of G then HK is a subgroup of G.\nA. True, True\nB. False, False\nC. True, False\nD. False, True\nAnswer: 
EOF
```

然后，创建联合推理服务，需要注意以下参数的设置：
- **hardExampleMining**：从 {BertRouter, Random, EdgeOnly, CloudOnly} 中设置用于边缘侧推理的难例挖掘算法
- **input_text**：指定用于推理的输入文本文件或目录路径。如果设置为目录，程序将扫描该目录中的所有文件
- **all_examples_inference_output**：设置推理结果的输出路径
- **hard_example_edge_inference_output**：设置边缘侧推理难例结果的输出路径
- **hard_example_cloud_inference_output**：设置云端推理难例结果的输出路径
- **MINING_MODE**：从 {inference-then-mining, mining-then-inference} 中设置挖掘模式。前者表示先进行推理，然后根据小模型的推理结果进行难例挖掘；后者表示先进行难例挖掘，然后根据挖掘结果进行推理
- **MODEL_LOAD_MODE**：从 {http, https, hf, file} 中设置模型加载模式。http 和 https 用于基于 API 的模型，hf 用于 HuggingFace 模型，file 用于本地模型
- **API_KEY**：（可选）为基于 API 的模型设置 API 密钥
- **MODEL_NAME**：（可选）为基于 API 的模型设置模型名称

```bash
CLOUD_NODE="cloud-node-name"
EDGE_NODE="edge-node-name"
API_KEY="your-api-key"

kubectl create -f - <<EOF
apiVersion: sedna.io/v1alpha1
kind: JointInferenceService
metadata:
  name: answer-generation-inference-example
  namespace: default
spec:
  edgeWorker:
    model:
      name: "answer-generation-inference-little-model"
    hardExampleMining:
      name: "BertRouter"
      parameters:
        - key: "model"
          value: "routellm/bert"
        - key: "threshold"
          value: "0.5"
    template:
      spec:
        nodeName: $EDGE_NODE
        dnsPolicy: ClusterFirstWithHostNet
        containers:
        - image: kubeedge/sedna-example-joint-inference-answer-generation-little:v0.8.0
          imagePullPolicy: IfNotPresent
          name:  little-model
          env:  # 用户自定义环境变量
          - name: "BACKEND_TYPE"
            value: "TORCH"
          - name: "MINING_MODE"
            value: "mining-then-inference"
          - name: "MODEL_LOAD_MODE"
            value: "hf"
          - name: "input_text"
            value: "/data/input"
          - name: "all_examples_inference_output"
            value: "/data/output"
          - name: "hard_example_cloud_inference_output"
            value: "/data/hard_example_cloud_inference_output"
          - name: "hard_example_edge_inference_output"
            value: "/data/hard_example_edge_inference_output"
          resources:  # 用户自定义资源
            requests:
              memory: 64M 
              cpu: 100m
            limits:
              memory: 2Gi
          volumeMounts:
            - name: outputdir
              mountPath: /data/
        volumes:   # 用户自定义卷
          - name: outputdir
            hostPath:
              # 用户必须在主机上创建该目录
              path: /joint_inference
              type: Directory

  cloudWorker:
    model:
      name: "answer-generation-inference-big-model"
    template:
      spec:
        nodeName: $CLOUD_NODE
        dnsPolicy: ClusterFirstWithHostNet
        containers:
          - image: kubeedge/sedna-example-joint-inference-answer-generation-big:v0.8.0
            name:  big-model
            imagePullPolicy: IfNotPresent
            env:
              - name: "BACKEND_TYPE"
                value: "TORCH"
              - name: "MODEL_LOAD_MODE"
                value: "https"
              - name: "API_KEY"
                value: $API_KEY
              - name: "MODEL_NAME"
                value: "deepseek-chat"
            resources:
              requests:
                memory: 2Gi
EOF
```

### 检查联合推理状态

```bash
kubectl get jointinferenceservices.sedna.io
```

![服务状态](https://raw.githubusercontent.com/kubeedge/sedna/main/examples/joint_inference/answer_generation_inference/images/service_status.png)

### 检查推理结果

你可以在 JointInferenceService 配置中定义的输出路径（例如 `/joint_inference/output`）检查推理结果。

```bash
root@edge0:/joint_inference# ls output
0.txt  1.txt  2.txt  3.txt

root@edge0:/joint_inference# cat output/0.txt
Input: The future of AI is
Output: The future of AI is here, and it's already starting to impact the way we live our lives...

root@edge0:/joint_inference# cat output/1.txt
Input: The president of the United States is
Output: The president of the United States is a very important person...

root@edge0:/joint_inference# cat output/2.txt
Input: There is a single choice question about abstract_algebra...
Output: ...Answer: D. 3

root@edge0:/joint_inference# cat output/3.txt
Input: There is a single choice question about abstract_algebra...
Output: ...Answer: C.
```

## 核心机制解读

### 系统架构
```
       用户提问
          │
          ▼
    ┌─────────────┐
    │ BertRouter  │ → 复杂度判别与路由
    └──────┬──────┘
           │
    ┌──────┴──────┐
    ▼             ▼
边缘轻量模型     云端大模型
(Qwen-0.5B)    (DeepSeek/GPT)
    │             │
    └──────┬──────┘
           ▼
       最终回答合成
```

### 推理模式选择

- **传统模式（Inference-then-Mining）**：该模式下系统会先在边缘侧使用轻量模型对输入进行完整推理生成结果，再根据生成结果的质量或置信度判断是否需提交至云端大模型重新处理。这种做法在图像或视频任务中较为常见，例如目标检测中先由边缘模型生成初步检测框，再根据框的置信度决定是否提交云端精细化识别。然而在文本生成场景中，该策略存在明显不足：文本生成通常需要逐词解码，生成完整回复本身即消耗大量计算和时间，尤其生成长文本时，边缘侧已付出较高成本，若事后判定质量不足再转云，将造成计算资源和响应时间的双重浪费，整体效率低下。

- **新模式（Mining-then-Inference）**：该策略在生成前先对输入问题进行复杂度分析与判别，依据预设规则或轻量分类模型（如BertRouter）快速决策应路由至边缘或是云端执行推理，从而避免不必要的生成开销。这一机制特别适合LLM文本场景：它仅在真正需要的时候才调用大模型，既减少了边缘设备的计算压力，也显著降低了响应延迟和云端调用成本，实现了效率与质量之间的更优平衡。

### 统一接口设计
通过 Estimator 类封装不同来源的模型，提供一致的 predict 接口，支持本地、HuggingFace、API等多种加载方式。

### 联合推理调用示例
```python
hard_example_mining = JointInference.get_hem_algorithm_from_config()
inference_instance = JointInference(
    estimator=Estimator,
    hard_example_mining=hard_example_mining
)
is_hard, final, edge_res, cloud_res = inference_instance.inference([text])
```

### 与以往方案的差异
- **任务扩展**：从图像/视频扩展到自然语言与多轮对话；
- **路由策略升级**：支持生成前判别，减少冗余计算；
- **模型管理增强**：统一支持 HuggingFace 与 API 类型的模型加载。

## 场景示例

```
# 边缘处理示例
Input: The president of the United States is
Output: The president of the United States is a very important person...
Node: Edge

# 云端处理示例
Input: There is a single choice question about abstract_algebra...
Output: Let's check each statement ... Answer: C.
Node: Cloud
```

## 小结

Sedna 联合推理通过边云协同，在LLM复杂多样的问题场景中实现：
- 成本降低65%以上（示例阈值下）；
- 回答质量可控，复杂问题自动提升处理层级；
- 多源模型统一接入，降低开发复杂度。

---
**项目地址**：[https://github.com/kubeedge/sedna](https://github.com/kubeedge/sedna)  
**设计文档**：[https://github.com/kubeedge/sedna/blob/main/docs/proposals/joint-inference-llm.md](https://github.com/kubeedge/sedna/blob/main/docs/proposals/joint-inference-llm.md)  
**示例代码**：[https://github.com/kubeedge/sedna/tree/main/examples/joint_inference/answer_generation_inference](https://github.com/kubeedge/sedna/tree/main/examples/joint_inference/answer_generation_inference)  
