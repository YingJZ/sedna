# 边云协同大语言模型推理：需求、挑战、方案与实践

## 为什么需要 LLM 模型边云协同推理

在实际应用中，大语言模型（LLM）的问题类型呈强烈的异质性：
- 简单事实类（“美国总统是谁？”）—— 只需少量上下文和参数即可生成稳定答案。
- 复杂开放类（“未来 AI 的发展方向是什么？”）—— 需要更强的语言建模与长程推理能力。
- 专业推理/学术题（“在 Z_3 中找出所有使得 Z_3[x]/(x^2+c) 成为域的 c 值”）—— 需要符号/逻辑能力与更长上下文窗口。

若全部请求都送往云端大模型：成本高、带宽占用大、延迟不可控；若全部在边缘：复杂问题质量显著下降。边云协同的目标即：在保证体验与准确性的前提下，最小化云端算力/调用成本与网络开销。Sedna 的联合推理机制通过“复杂度评估 + 分级路由”实现这一平衡。

## 技术挑战

1. 路由判别的有效性与实时性：传统 Ianvs 中偏重视觉场景的“先推理后再判定是否升级”策略在 LLM 语义场景下会产生冗余计算，尤其文本问题无需在边缘先完整生成再决定是否转云端。
2. 模型异构与加载统一抽象：边缘可能运行本地权重（HuggingFace / 文件系统），云端可能是 API（DeepSeek / OpenAI）。需要一套抽象屏蔽差异，支持最小侵入集成。

## 挑战解决思路（摘要）

1. 引入 Mining-then-Inference 模式：在生成前先做复杂度判别（BertRouter），避免不必要的边缘完整推理。  
2. 设计 Estimator 抽象与多态 LLM 适配层：通过统一 `predict` 接口与加载模式枚举（file/hf/https），实现“同一调用代码，多种部署后端”。  。

## 效果 / 性能比较

以阈值 0.46、1000 条混合 MMLU 风格问题测试：
- 65.4% 请求在边缘完成，平均响应 <0.5s。
- 云端 API 调用成本较“全云”策略下降 ~65%。
- 系统端到端平均延迟下降 ~45%。

阈值敏感性对比（示例）：
| 阈值 | 简单比例 | 复杂比例 | 边缘 token | 云端 token |
|------|----------|----------|------------|------------|
| 0.45 | 40.50%   | 59.50%   | 38.09%     | 61.91%     |
| 0.46 | 65.40%   | 34.60%   | 56.76%     | 43.24%     |
| 0.47 | 94.30%   | 5.70%    | 95.54%     | 4.46%      |

运营策略：提高阈值 → 更多边缘处理 → 成本/延迟下降；降低阈值 → 更多云端参与 → 复杂问答质量提升。

## 使用介绍 / 说明

1. 安装 Sedna：参考官方文档或执行（示例）
```bash
kubectl apply -f sedna-installation.yaml
```
2. 创建模型 CRD：分别注册云端（https API）与边缘（hf）模型。
```bash
kubectl create -f - <<EOF
apiVersion: sedna.io/v1alpha1
kind: Model
metadata:
  name: answer-generation-inference-big-model
spec:
  url: "/api.deepseek.com/v1"
  format: "https"
EOF

kubectl create -f - <<EOF
apiVersion: sedna.io/v1alpha1
kind: Model
metadata:
  name: answer-generation-inference-little-model
spec:
  url: "/Qwen/Qwen2.5-0.5B-Instruct"
  format: "hf"
EOF
```
3. 部署联合推理服务：设定 `MINING_MODE` 为 `mining-then-inference`，并传入阈值。
```bash
kubectl create -f - <<EOF
apiVersion: sedna.io/v1alpha1
kind: JointInferenceService
metadata:
  name: answer-generation-inference-example
spec:
  edgeWorker:
    model:
      name: "answer-generation-inference-little-model"
    hardExampleMining:
      name: "BertRouter"
      parameters:
        - key: "threshold"
          value: "0.46"
    template:
      spec:
        containers:
        - image: kubeedge/sedna-example-joint-inference-answer-generation-little:v0.8.0
          env:
          - name: "MINING_MODE"
            value: "mining-then-inference"
          - name: "MODEL_LOAD_MODE"
            value: "hf"
  cloudWorker:
    model:
      name: "answer-generation-inference-big-model"
    template:
      spec:
        containers:
        - image: kubeedge/sedna-example-joint-inference-answer-generation-big:v0.8.0
          env:
          - name: "MODEL_LOAD_MODE"
            value: "https"
          - name: "API_KEY"
            value: $API_KEY
EOF
```
4. 准备输入与查看输出：
```bash
mkdir -p /joint_inference/input
cat > /joint_inference/input/test.txt <<'EOF'
The future of AI is
The president of the United States is
There is a single choice question about abstract_algebra...
EOF
ls /joint_inference/output/
```

## 技术细节

### 架构概览
```
┌────────── 用户问题 ──────────┐
             │
             ▼
      ┌─ BertRouter 判复杂度 ─┐
      │  分级/阈值决策        │
      └─────────┬───────────┘
                │
        ┌───────┴────────┐
        │                │
     边缘轻量          云端大模型
     Qwen-0.5B         DeepSeek / GPT
        │                │
        └───────┬────────┘
                ▼
            合成最终回答
```

### 双模式对比
- Inference-then-Mining：先生成再判断 → 视觉类任务可复用，但在文本类长生成场景有冗余。
- Mining-then-Inference：先复杂度评估后选择模型 → 减少无效 token 开销，是 LLM 场景推荐模式。

### 统一模型抽象
```python
class Estimator:
    def __init__(self, **kwargs):
        self.load_mode = os.environ.get("MODEL_LOAD_MODE", "file")
        if self.load_mode in ("http", "https"):
            self._llm = APIBasedLLM(**kwargs)
        elif self.load_mode == "hf":
            self._llm = HuggingFaceLLM(**kwargs)

class APIBasedLLM(BaseLLM):
    def predict(self, data, **kwargs):
        messages = [{"role": "user", "content": data[0]}]
        completion = self.client.chat.completions.create(
            model=self.model_name,
            messages=messages
        )
        return [completion.choices[0].message.content]
```

### 边缘侧流程片段
```python
hard_example_mining = JointInference.get_hem_algorithm_from_config()
inference_instance = JointInference(
    estimator=Estimator,
    hard_example_mining=hard_example_mining
)
is_hard, final, edge_res, cloud_res = inference_instance.inference([text])
```

### 阈值运营表与策略
通过历史日志收集：问题复杂度分布 / token 消耗 / 质量评估（如 BLEU、人工标签），自动回写监控面板，供运维与成本控制参考。后续计划引入强化学习或多臂老虎机方式做自适应阈值调节。

### 与 Ianvs 对比（核心差异）
- 侧重点迁移：从图像/视频场景扩展到自然语言语义与多轮问答。
- 路由策略改进：从“推理后再升级”扩展到“生成前判别 + 多模式可选”。
- 模型管理强化：原先示例多为本地权重；现支持 HuggingFace + HTTPS API 的统一 CRD 声明与可扩展加载。

## 场景示例
```bash
# 边缘处理示例
Input: The president of the United States is
Output: The president of the United States is a very important person...
Node: Edge

# 云端处理示例
Input: There is a single choice question about abstract_algebra...
Output: Let's check each statement ... Answer: C.
Node: Cloud
```

## 未来规划
1. 模型 CRD 原生支持 API/HF 元数据，无需临时空目录。参见 Issue #483。
2. 自适应阈值：融合网络状态、排队长度、调用费用。
3. 多模态与流式输出：文本 + 图像输入，增量 token 回传。

## 总结
Sedna 联合推理方案将边缘快速响应与云端高质量生成结合，在 LLM 复杂度差异场景下提供一条可运营、可调参、低成本的演进路径：
- 成本下降 ~65%（示例阈值 0.46）
- 延迟显著降低（大多数简单问答本地完成）
- 质量可控（复杂问题自动云端升级）
- 接口统一（多源模型一致调用）

欢迎试用、反馈与共建！

## 参考资源
1. 项目主页: https://github.com/kubeedge/sedna  
2. 设计文档: joint-inference-llm.md  
3. 示例代码: examples/joint_inference/answer_generation_inference  
4. Ianvs 原始示例: cloud-edge-collaborative-inference-for-llm

**作者**: KubeEdge-Sedna 社区  
**许可**: Apache License 2.0  
> 如果觉得文章有帮助，欢迎点赞 / 分享 / 关注。

